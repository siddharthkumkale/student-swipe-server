<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Keeps the foreground inside the adaptive-icon safe circle (avoids clipping / off-center look). -->
  <item
      android:bottom="22dp"
      android:drawable="@drawable/ic_launcher_foreground"
      android:left="22dp"
      android:right="22dp"
      android:top="22dp" />
</layer-list>
